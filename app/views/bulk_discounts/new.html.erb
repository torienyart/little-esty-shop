<h1>New Bulk Discount</h1>
<%= form_with model: BulkDiscount.new, url: [@merchant, BulkDiscount], local: true do |f| %>
<%= f.label :quantity_threshold%>
<%= f.text_field :quantity_threshold%>

<%= f.label :percentage_discount%>
<%= f.text_field :percentage_discount%>

<%= f.submit 'Create Bulk Discount' %>
<%end%>